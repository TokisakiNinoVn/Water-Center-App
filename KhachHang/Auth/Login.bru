meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{domainUrl}}/customer/login
  body: json
  auth: inherit
}

headers {
  Accept: application/json
}

body:json {
  {
      "phone": "0987654434",
      "password": "Abc@123456"
  }
}

script:post-response {
  const json = res.getBody();
  
  if (json?.data?.access_token) {
    bru.setEnvVar("tokenKhachHang", json.data.access_token);
  }
  
}

settings {
  encodeUrl: true
  timeout: 0
}
